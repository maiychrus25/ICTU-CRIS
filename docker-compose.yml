services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: cris
      POSTGRES_PASSWORD: cris
      POSTGRES_DB: cris
    ports: ["5432:5432"]
    volumes:
      - ./docker/initdb:/docker-entrypoint-initdb.d
      - cris_pg:/var/lib/postgresql/data
volumes:
  cris_pg: {}
