# Lát cắt O — danh bạ giảng viên và làm lộ đủ các loại công trình

Ngày 19/09/2026. Hai yêu cầu của anh:
1. Chưa có trang và mục sidebar dẫn sang **giảng viên** (kho nguồn có menu "Giảng viên", 410 người).
2. "Trên prod chỉ thấy bài báo, trong khi repository.ictu.edu.vn có đủ các loại."

## Chẩn đoán yêu cầu 2 (đo trên prod 19/09/2026, v0.7.0)
Dữ liệu **không thiếu**: `GET /api/about` → `works_by_type` = bài báo 1.907 · đồ án 5.375 · luận văn 323 · luận án 11 ·
học liệu 2 (tổng 7.618), khớp từng con số với trang chủ kho nguồn; giảng viên 400 hồ sơ (10 hồ sơ trùng ORCID ở nguồn bị
bỏ qua — lỗi có sẵn). Lỗi nằm ở **khả năng nhìn thấy**:
- `GET /api/works` xếp `year_issue DESC NULLS LAST` — đồ án/luận văn/luận án ở kho **không ghi năm** nên nằm sau 39 trang
  bài báo; trang 1 mặc định là 50/50 bài báo.
- Tra cứu chỉ có select "Loại tài liệu: Tất cả" (không có số lượng); kho nguồn có **menu riêng từng loại**.
- Tổng quan: thẻ "Tổng công trình 5 năm 1.133" và biểu đồ theo năm chỉ có bài báo (cùng lý do thiếu năm); không nơi nào
  nói kho có 5.375 đồ án.

## Hợp đồng API

| Endpoint | Thay đổi |
|---|---|
| `GET /api/works/facets` | thêm `doc_types: [{value: "bai_bao"\|"do_an"\|"luan_van"\|"luan_an"\|"hoc_lieu", label, n}]` — đủ 5 loại kể cả n=0, thứ tự cố định như trên; nhãn: Bài báo · Đồ án/Khoá luận · Luận văn ThS · Luận án TS · Học liệu số |
| `GET /api/works` | thứ tự mặc định đổi thành `year_issue DESC NULLS LAST, cohort_num DESC NULLS LAST, id DESC` (đồ án theo khoá mới nhất trước; `cohort` là text như "K21"/"21" → so theo số). Thêm `sort=recent` (mặc định, như trên) \| `title` (tiêu đề A→Z, `title_norm` nếu có) \| `added` (mới đưa vào kho trước: `first_seen_at` của bản ghi nguồn DESC). Giá trị khác → 422. CSV nhận cùng tham số |
| `GET /api/stats` | thêm `totals: {works: int, persons: int, by_type: {bai_bao, do_an, luan_van, luan_an, hoc_lieu}}` (toàn bộ kho, không giới hạn 5 năm; chỉ công trình đang hoạt động, bỏ bản đã gộp — cùng điều kiện với `/api/works`) |
| `GET /api/persons/directory` (mới, công khai như các endpoint đọc khác) | tham số `q` (tên, không dấu cũng được — tái dùng logic `search_persons`), `unit` (mã hoặc id khoa; `none` = chưa gán khoa), `degree` (`gs`\|`pgs`\|`ts`\|`ths`\|`other`), `has_works` (`true` chỉ người có công trình đã liên kết), `sort` (`works` mặc định: nhiều công trình trước \| `name`: theo **tên gọi** tiếng Việt — từ cuối của họ tên — rồi họ), `page` (1…), `per_page` (mặc định 24, ≤ 60). Trả `{items: [DirectoryPerson], page: {page, per_page, total}, facets: {units: [{value, label, n}], degrees: [{value, label, n}]}}`. `DirectoryPerson` = `{id, display_name, rank, degree, position, unit: {id, code, name}\|null, field, avatar_url, orcid, works, by_type: {bai_bao, do_an, luan_van, luan_an, hoc_lieu}}` (`works` = số công trình đã liên kết `DaNoiTuDong`/`DaXacNhan`; `by_type` chỉ gồm loại > 0). **Không** có email/điện thoại/ngày sinh. Chỉ `kind='lecturer'`, `active`. Facet đếm trên tập đã lọc theo `q` nhưng **chưa** lọc theo chính facet đó |

Phải khai báo route `/persons/directory` **trước** `/persons/{pid}` trong router (tránh bị bắt nhầm là pid).

## O1 — Backend (sonnet)
- `cris/api/routes/search.py`: facet `doc_types`, thứ tự mặc định mới + `sort`; `cris/api/routes/export.py` nhận `sort`.
- `cris/api/routes/stats.py` (hoặc nơi có `/api/stats`): `totals`.
- `cris/api/routes/persons.py`: `/persons/directory` — một truy vấn gộp (CTE đếm công trình theo loại từ `v_person_publications`), không N+1; phân trang ổn định (thêm `p.id` vào ORDER BY).
- `cris/api/schemas.py`: `DocTypeFacet` (hoặc tái dùng kiểu facet hiện có), `StatsTotals`, `DirectoryPerson`, `PersonDirectoryOut`.
- Không migration nếu không cần; nếu sắp theo tên gọi cần cột thì tính trong SQL (`split_part`/regexp) — không thêm cột.
- Test ≥ 12: facet đủ 5 loại kể cả 0; thứ tự mặc định đưa đồ án khoá lớn lên trước đồ án khoá nhỏ và sau công trình có năm; `sort=title|added`, `sort` lạ → 422; `totals` khớp đếm trực tiếp và bỏ bản đã gộp; directory: phân trang + total, lọc `unit`/`unit=none`/`degree`/`has_works`, `q` không dấu, `sort=name` theo tên gọi, facet không tự lọc, payload không có email/phone/dob, route không đụng `/persons/{pid}`.
- Tài liệu: `CHANGELOG.md [Unreleased]`, `docs/SRS.md` (FR mới), "## Kết quả O1" cuối file này (số thật trên DB local).
- Commit: `feat(api): lecturer directory endpoint, document-type facet, whole-repository totals and a default order that surfaces theses`.

## Codex O — giao diện
- **Trang danh bạ `/giang-vien/`** (chính route hiện có): khi **không có `?id=`** thì hiện danh bạ thay cho EmptyView "Chưa chọn giảng viên"; có `?id=` giữ nguyên hồ sơ (thêm liên kết "← Danh bạ giảng viên"). Danh bạ: ô tìm tên (debounce, không dấu), lọc Khoa · Học hàm/học vị · "Chỉ người có công trình", sắp xếp (Nhiều công trình · Theo tên), lưới thẻ (1 cột ở 390, 2 ở 820, 3–4 ở ≥1280): `PersonAvatar`, tên kèm học hàm/học vị viết đúng ("PGS.TS.", "ThS." — tách hàm định dạng dùng chung với hồ sơ và hàng đợi tác giả vào `lib/`), chức vụ, khoa (mã — tên), lĩnh vực, số công trình + phân theo loại dạng chip nhỏ; cả thẻ là liên kết tới hồ sơ; phân trang; trạng thái tải/rỗng/lỗi; đồng bộ bộ lọc lên URL (`?q=&unit=&degree=&sort=&page=`); tiêu đề có tổng số ("Giảng viên · 400").
- **Sidebar**: thêm mục **"Giảng viên"** (icon `Users`, `public: true`) ngay dưới "Tra cứu"; mục active khi ở `/giang-vien/` (kể cả hồ sơ và lý lịch). Dưới "Tra cứu" thêm nhóm con thụt lề, thu gọn được, mặc định mở: Bài báo · Đồ án/Khoá luận · Luận văn · Luận án · Học liệu số → `/tra-cuu/?doc_type=…`, kèm số lượng từ `facets.doc_types` (ẩn số khi chưa tải); mục con active theo `doc_type` trên URL. Sidebar không được tràn/che nút "Kiểm tra đề tài" ở màn thấp (cuộn trong vùng nav).
- **Tra cứu**: thay select "Loại tài liệu" bằng **hàng tab loại** ngay trên ô tìm: Tất cả (7.618) · Bài báo (1.907) · Đồ án/Khoá luận (5.375) · Luận văn (323) · Luận án (11) · Học liệu số (2); cuộn ngang trong chính hàng tab ở 390px, không tràn trang; đổi tab = đổi `doc_type` trên URL và về trang 1. Thêm select "Sắp xếp" (Mới nhất · Tiêu đề A→Z · Mới đưa vào kho). Bộ lọc chỉ áp cho bài báo (Loại nơi công bố, Chỉ mục, Điểm, Quartile) ẩn khi tab khác Bài báo/Tất cả; "Khoá" hiện khi tab Đồ án/Tất cả. Cột "Năm" với đồ án hiện "Khoá 21" khi không có năm.
- **Tổng quan**: hàng thẻ "Toàn bộ kho" đặt **trên** hàng 5 năm: Tổng công trình · Bài báo · Đồ án/Khoá luận · Luận văn · Luận án · Học liệu số · Giảng viên (từ `stats.totals`), mỗi thẻ bấm được (→ tra cứu theo loại / danh bạ). Dưới biểu đồ đổi câu "5.734 công trình không rõ năm" thành giải thích: "… đồ án, luận văn và luận án ở kho nguồn không ghi năm nên không nằm trong biểu đồ" + liên kết "Xem theo khoá".
- **Trang chủ công khai `/`** và `/ve/`: nếu có số liệu theo loại thì dùng cùng nguồn.
- Trường mới đều optional: backend cũ chưa trả `doc_types`/`totals`/`directory` thì giao diện rơi về cách cũ, không vỡ.
- `lib/types.ts`, `lib/api.ts` (+ mock cho directory có lọc/phân trang/sort), `lib/queries.ts`, `lib/fixtures.ts` (≥ 30 giảng viên mẫu đủ khoa/học vị, có người không ảnh/không khoa); `app/sitemap.ts` giữ `/giang-vien/`; metadata SEO trang danh bạ (index được). e2e mới `e2e/phase-o.spec.ts` ≥ 5 kịch bản (nav Giảng viên, tìm + lọc danh bạ, mở hồ sơ rồi quay lại giữ bộ lọc, tab loại ở Tra cứu, thẻ toàn kho ở Tổng quan); cập nhật spec cũ nếu bố cục đổi; `node scripts/ui-audit.mjs` sạch cho `/giang-vien/`, `/tra-cuu/`, `/tong-quan/` (thêm `/giang-vien/` không id vào danh sách route của script).

## Kết quả
(điền sau)
