# Kế hoạch: lát cắt J (AI hướng ra người dùng cuối, bản đồ tri thức) và K (ngang kho gốc ở phần công khai, cảnh báo bất thường, lý lịch khoa học)

Ngày 12/09/2026, sau v0.4.0 chạy thật. Anh duyệt toàn bộ đề xuất; yêu cầu **UI dễ dùng**.
Ràng buộc chung như các kế hoạch trước (tầng nghiệp vụ không sửa — thêm module; AI chỉ ghi
`ai_*`/`ai_query`; test Docker hai biến DB, pytest tiền cảnh; commit tiếng Anh không trailer
AI; backend sonnet, UI Codex). Backend tuần tự J1 → J2 → K1 → K2 (cùng sửa `app.py`/
`schemas.py`); Codex làm UI theo **hợp đồng API ghi sẵn dưới đây** với fixture, kiểm lại trên
backend thật khi backend xong.

## Hợp đồng API (Codex dựng UI theo đây; backend phải khớp)

| Endpoint | Vào | Ra |
|---|---|---|
| `GET /api/works?…&mode=semantic&q=` | như tra cứu; `mode=semantic` bật tìm theo nghĩa (cần AI; nếu tắt → `note` + rơi về từ khoá) | `WorkList` + `mode`, `note?`; mỗi item thêm `score?` (cosine 0–1) |
| `POST /api/ai/experts` | `{title, description?, aspects?, k?=10, exclude_person_ids?: [], min_degree?: "TS"\|"ThS"\|null, unit?: id, recent_years?: 3}` | `{query_id, provider, fallback, note, results:[{person_id, display_name, degree, unit_code, score, works_matched, evidence:[{work_id,title,doc_type,year,score}] }]}` |
| `GET /api/ai/experts/{query_id}` | — | như trên (đã lưu) |
| `POST /api/public/check-topic` | `{title, description?}` — **không cần đăng nhập**, giới hạn 20 lượt/5 phút/IP | `{similar:[{work_id,title,doc_type,cohort,year,score,level}], experts:[{person_id,display_name,degree,unit_code,score}], note}` (top 8 + 5, không lưu `ai_query`, không lộ email) |
| `GET /api/ai/map?color=topic\|unit\|year\|doc_type` | — | `{points:[{id, x, y, topic_id, unit_id, year, doc_type, title}], topics:[{id,label,size,cx,cy}], built_at, method:"pca"}` — 2 chiều PCA từ vector (numpy), toạ độ chuẩn hoá −1..1; cache trong `ai_map` (jsonb) tính bởi `python -m cris ai map` |
| `GET /api/ai/trends?by=cohort\|year` | — | `{series:[{topic_id,label,values:[{key,count,share}]}], keys:[...]}` cho 12 cụm lớn nhất + "khác" |
| `GET /api/ai/coauthors?min_works=2` | — | `{nodes:[{person_id,display_name,unit_code,works}], edges:[{a,b,weight}]}` (giảng viên–giảng viên cùng công trình đã liên kết) |
| `GET /api/works/{id}/citation?style=apa\|ieee\|bibtex` | — | `text/plain` (bibtex: `application/x-bibtex`, `Content-Disposition` khi `download=1`) |
| `GET /api/works/{id}` | — | thêm `pdf_url`, `source_url`, `keywords: string[]` (tách từ `keywords_raw`) |
| `GET /api/works?pub_type=&quartile=&cohort=&keyword=` | bộ lọc mới; `GET /api/works/facets` → `{pub_types:[{value,label,n}], quartiles, cohorts, years, units}` | |
| `GET /api/persons/{id}` | — | thêm `rank` (học hàm), `scholar_url`, `citation_stats?` (null nay) |
| `GET /api/persons/{id}/cv?format=html` | — | HTML in được (lý lịch khoa học: thông tin, số liệu, công trình theo loại/năm, DOI, ORCID) — trang UI `/giang-vien/ly-lich/?id=` render và `window.print()` |
| `GET /api/recent?limit=20` | — | `{added:[WorkSummary+first_seen_at], changed:[…+version], run: LastSync}` từ `source_record` của lượt đồng bộ gần nhất |
| `GET /api/feed.xml` | — | RSS 2.0 20 công trình mới nhất (không cần đăng nhập) |
| `GET /api/quality/anomalies?kind=&page=` + `POST /api/quality/anomalies/{id}/dismiss {reason}` | — | `{items:[{id, kind, kind_label, work_id, title, detail, severity, state}], page}`; kinds: `scopus_no_doi`, `year_out_of_range`, `thesis_title_equals_article`, `orcid_duplicate`, `doi_invalid`, `missing_abstract_article`; bảng `quality_flag` (migration `0014`), tính bởi `python -m cris quality scan` |

## J1 — Backend AI hướng ra người dùng (`cris/ai/search.py`, `cris/ai/expert.py`, `cris/api/routes/ai_public.py`)
- Tìm kiếm ngữ nghĩa: embed `q` → top-k trên `load_matrix` → lọc tiếp bằng `_works_query` (doc_type/year/unit…) → sắp theo score; `note` "Tìm theo nghĩa (AI): kết quả có thể không chứa từ đã gõ".
- Chuyên gia: điểm `Σ s_i·0,8^hạng` trên top‑200 công trình gần nhất, gom theo `person_id` qua `v_person_publications` (link sống), hệ số ×1,1 nếu bài ≤ `recent_years`, lọc `min_degree` theo `degree_raw` (TS/PGS/GS ≥ TS; ThS), `unit`, loại trừ. Lưu `ai_query(kind='experts')` (nếu `ai_query` chưa có cột `kind` → migration `0014` thêm `kind text NOT NULL DEFAULT 'compare'`). Test ≥ 8 (FakeProvider): người có 2 bài liên quan > người có 1; loại trừ; lọc học vị; fallback khi AI tắt (409 hoặc `fallback=true` với kết quả theo từ khoá).
- Cổng sinh viên: `POST /api/public/check-topic` không actor; rate-limit theo IP (dict tiến trình như login); trả `similar` (từ `compare_topic` không lưu — thêm tham số `save=False` vào `compare_topic`? **không sửa compare.py**: gọi phần `_semantic_results` qua hàm public mỏng mới trong `search.py`) + `experts` top 5.
- Đo: script `scripts/eval_experts.py` — 30 đồ án có GVHD thật (ngẫu nhiên, seed 0), che GVHD, hỏi experts → tỷ lệ GVHD nằm top‑5; in số vào `docs/ai.md`.

## J2 — Backend bản đồ & xu hướng (`cris/ai/map.py`, `cris/ai/trends.py`, `cris/ai/coauthors.py`)
- PCA 2 chiều bằng numpy SVD trên ma trận vector (7.618×384, ~1 s); lưu `ai_map(id, model, built_at, points jsonb)`; CLI `ai map`. Điểm mang `topic_id` từ `topic_of_work` (đã có, tính một lần khi build).
- Xu hướng: đếm công trình theo (cụm, khoá) và (cụm, năm) từ `ai_topic_keyword` khớp `keywords_raw` (cùng cách như bộ lọc `topic`); `share` = tỉ lệ trong khoá/năm.
- Đồng tác giả: cặp `person_id` cùng `work_id` qua liên kết sống, `weight` = số công trình chung.
- Test ≥ 6.

## K1 — Backend ngang kho gốc (`cris/cite.py`, `cris/api/routes/{search,persons,recent}.py`)
- Trích dẫn APA 7 / IEEE / BibTeX từ metadata (tác giả từ `author_mention` vai author theo `position`, năm, tiêu đề, tạp chí, tập/số, DOI; đồ án: sinh viên, GVHD, "Đồ án tốt nghiệp, ICTU"). Test 6 (đủ/thiếu trường, dấu tiếng Việt, BibTeX key).
- `pdf_url`/`source_url`/`keywords[]` ở chi tiết; bộ lọc `pub_type`/`quartile`/`cohort`/`keyword` + `/api/works/facets`; `rank`/`scholar_url` ở hồ sơ (đọc từ `person`/`source_record.archive.rank`, `scholar`); `/api/persons/{id}/cv` HTML (Jinja? **không** — f-string template trong `cris/cv.py`, escape bằng `html.escape`); `/api/recent`; `/api/feed.xml` (`email.utils.format_datetime`, escape XML).
- Test ≥ 10.

## K2 — Backend cảnh báo bất thường (`cris/anomaly.py`, migration `0014`/`0015`)
- Quét 6 loại như bảng; `quality_flag(id, kind, work_id, person_id, detail jsonb, severity, state 'open'|'dismissed', dismissed_by, reason, created_at)`, UNIQUE(kind, work_id, person_id); quét idempotent, đóng cờ khi hết bất thường; CLI `quality scan`; `dismiss` ghi audit `quality.dismiss`. Test ≥ 8.

## Codex đợt J (UI — dễ dùng là tiêu chí số 1)
1. **Tra cứu**: công tắc "Theo từ khoá / Theo nghĩa (AI)" ngay cạnh ô tìm; chip từ khoá bấm được ở kết quả; bộ lọc mới (loại bài/chỉ mục, quartile, khoá) dạng thanh lọc gọn với số lượng từ `facets`; nút "Mở PDF ở kho" và "Trích dẫn" (Dialog 3 tab APA/IEEE/BibTeX, nút sao chép, tải `.bib`) ở chi tiết công trình.
2. **Tìm chuyên gia** — tab thứ ba của "Đối chiếu đề tài": form (tiêu đề, mô tả, lọc học vị/đơn vị, loại trừ người bằng combobox), kết quả thẻ giảng viên: điểm dạng thanh, số bài liên quan, 3 dẫn chứng có điểm, nút "Mở hồ sơ", "Sao chép danh sách"; dòng nhắc "gợi ý trên tóm tắt, người quyết".
3. **Cổng kiểm tra đề tài** `/kiem-tra-de-tai/` — trang **công khai, không sidebar**, một ô nhập lớn + nút, kết quả: "Đề tài tương tự các khoá trước" (mức cao/vừa/thấp) + "Giảng viên gần chuyên môn"; link ở trang đăng nhập và footer; giới hạn lượt hiển thị thân thiện khi 429.
4. **Bản đồ tri thức** `/ban-do/`: canvas 2D (HTML canvas, không thư viện) 7.618 điểm, tô màu theo chọn (chủ đề/đơn vị/năm/loại), zoom/pan bằng chuột và chạm, hover hiện tiêu đề, click mở chi tiết, nhãn cụm tại tâm, chú giải; tab "Xu hướng" (biểu đồ vùng xếp chồng theo khoá/năm, chọn cụm), tab "Đồng tác giả" (đồ thị lực đơn giản tự viết hoặc bố cục vòng, kích thước nút = số công trình, bấm ra hồ sơ). Sidebar: mục "Bản đồ tri thức" sau "Chủ đề".
5. **Hồ sơ giảng viên**: học hàm, link Google Scholar, nút "Lý lịch khoa học" → `/giang-vien/ly-lich/?id=` (trang in: A4, `@media print`, nút In/PDF), nút trích dẫn từng bài.
6. **Tổng quan**: khối "Mới cập nhật từ kho" (thêm/đổi từ `/api/recent`) + link RSS.
7. **Cảnh báo bất thường**: trang `/chat-luong-du-lieu/canh-bao/` (tab trong Chất lượng dữ liệu): bảng theo loại, mức độ, nút "Bỏ qua (lý do)", link công trình.
Fixture cho mọi hợp đồng; Playwright ≥ 6 kịch bản; kiểm real khi backend xong.

## Ngoài kế hoạch
OpenAlex/Crossref (#7) — lát cắt riêng khi có cache và giới hạn tốc độ; SSO; biểu mẫu Bộ.

## Kết quả (12/09/2026)

| Việc | Commit |
|---|---|
| J1 — Tìm kiếm ngữ nghĩa, tìm chuyên gia, cổng công khai kiểm tra đề tài | `068f23b` |
| J2 — Bản đồ tri thức (PCA), xu hướng chủ đề, đồ thị đồng tác giả | `baf30c0` |
| K1 — Trích dẫn, bộ lọc/facets công trình, lý lịch khoa học, mới cập nhật, RSS | `f7518fb` |
| K2 — Cảnh báo bất thường dữ liệu | `22d1c5b` |
| Codex — UI đợt J mục 1–4, 6 (tra cứu ngữ nghĩa/facets/trích dẫn, tìm chuyên gia, cổng sinh viên, bản đồ + xu hướng + đồng tác giả, mới cập nhật) | `c1b9d02` |
| Codex — UI mục 5 (nút "Lý lịch khoa học" + trang `/giang-vien/ly-lich/?id=`) và mục 7 (trang cảnh báo bất thường) | chưa có commit — đang làm trong `frontend/` lúc viết dòng này, không đụng tới |

### Điều lệch so với kế hoạch

- **Số hiệu migration khác dự đoán ở hợp đồng và mục K2**: hợp đồng ghi `quality_flag`
  migration `0014` (hàng cuối bảng hợp đồng), mục K2 ghi `0014`/`0015` — số thật do bốn
  migration của cả J+K cấp phát tuần tự theo đúng thứ tự triển khai J1→J2→K1→K2:
  `0014_ai_query_kind.sql` (J1, cột `kind` của `ai_query`), `0015_ai_map.sql` (J2),
  `0016_person_rank.sql` (K1), `0017_quality_flag.sql` (K2). Không phải lỗi — chỉ là số dự
  đoán trước khi biết J1/J2/K1 đã tiêu bao nhiêu số migration trước K2.
- **`units` được thêm vào `GET /api/ai/map`**: hợp đồng đầu tài liệu chỉ định nghĩa
  `{points, topics, built_at, method}` — bản triển khai thêm trường `units` (đơn vị active
  có ít nhất một điểm, tính lại mỗi lần gọi chứ không cache trong `ai_map`) để giao diện
  hiện tên đơn vị thay vì id; xem `docs/ai.md` mục 10.
- **`pub_type` thực chất khớp cột `indexes`**: bộ lọc `GET /api/works?pub_type=` không có
  cột `pub_type` riêng trong `work` — khớp vào mảng `indexes` (Scopus/WoS/…); tên tham số
  giữ nguyên `pub_type` đúng như hợp đồng đã chốt với Codex, nhưng ý nghĩa thật là "chỉ
  mục", không phải một trường phân loại loại xuất bản riêng của `work`.
- **`orcid_duplicate` luôn đo được 0 trên dữ liệu hiện có**: đúng như giới hạn ghi sẵn
  trong docstring `_find_orcid_duplicate` (`cris/anomaly.py`) — ràng buộc `UNIQUE(orcid)`
  chặn hai `person` khác nhau cùng ORCID ngay từ lúc nhập (10/410 hồ sơ bị từ chối vì lý do
  này lúc `people`, xem "Đã biết" bản 0.4.0), nên cờ này chỉ có tác dụng phòng hờ khi ràng
  buộc bị nới lỏng sau này — không phải một loại bất thường chưa quét đúng.
- **Phiên triển khai bị ngắt và nối lại**: quá trình viết mã J+K trải qua hơn một phiên làm
  việc của agent (giới hạn ngữ cảnh), nên các commit backend/API/frontend rải trong ngày
  12/09 thay vì dựng liền một lượt — không ảnh hưởng nội dung bàn giao, chỉ giải thích vì
  sao lịch sử commit tách nhỏ hơn một lát cắt liền mạch điển hình.
