# Kế hoạch: lát cắt I — CI/CD lên máy chủ thật, tệp minh chứng, gợi ý người hướng dẫn bằng AI, hướng dẫn sử dụng

Ngày 11/09/2026, sau khi `https://cris.ahvlabs.com` chạy (v0.3.0, DB nạp từ bản chụp local).
Mục tiêu: (1) mỗi bản phát hành tự lên máy chủ, có sao lưu và quay lui; (2) minh chứng là
tệp thật chứ không chỉ URL; (3) AI xử lý nỗi đau lớn nhất của kho — 4.621/5.375 đồ án ghi
người hướng dẫn là `ICTU_TEACHER`; (4) người dùng mới tự xem được hướng dẫn.

Ràng buộc chung như các kế hoạch trước (tầng nghiệp vụ không sửa — thêm module; test Docker
hai biến DB; commit tiếng Anh không trailer AI; UI Codex, backend sonnet). Thứ tự: I1 ∥ I2 ∥
Codex I0 → I3 → Codex I (UI cho I2+I3).

## I1 — CI/CD triển khai máy chủ thật (`deploy.yml`)

- Trên VPS: người dùng `deploy` (không sudo, thuộc nhóm `docker`), sở hữu `/opt/ictu-cris`;
  khoá SSH riêng cho GitHub Actions (ed25519, chỉ `authorized_keys` của `deploy`, kèm
  `restrict` không cần vì cần shell); root vẫn giữ cho vận hành tay.
- Script `/opt/ictu-cris/deploy/upgrade.sh <tag>` (idempotent, chạy được bằng tay):
  1. `git fetch --tags && git checkout <tag>`;
  2. ảnh: `docker pull ghcr.io/maiychrus25/ictu-cris:<ver>-ai` — nếu không có (registry
     404) thì `docker build -t ictu-cris:<ver>-ai --build-arg EXTRAS="[ai]" .`;
  3. sao lưu `pg_dump -Fc` vào `backups/pre-<tag>-<ngày>.dump`;
  4. ghi `image:` mới vào `deploy/docker-compose.override.yml` (giữ bản cũ ở `.prev`);
  5. `docker compose run --rm -T app migrate` → `docker compose up -d app` → chờ
     `/api/health` 200 trong 60 s;
  6. thất bại → khôi phục override `.prev`, `up -d app`, thoát mã 1 (DB không quay lui —
     migration chỉ thêm, ghi rõ trong doc); thành công → `docker image prune -f`.
- `.github/workflows/deploy.yml`: `on: release: types [published]` + `workflow_dispatch`
  (input `tag`); job `deploy` `needs` ảnh `-ai` tồn tại (poll `docker manifest inspect` tối
  đa 10 phút vì `docker.yml` chạy song song); SSH bằng `appleboy/ssh-action` **hoặc** ssh
  thuần với `webfactory/ssh-agent` (chọn thuần để ít phụ thuộc); secrets `PROD_HOST`,
  `PROD_USER`, `PROD_SSH_KEY`, `PROD_KNOWN_HOSTS`; environment `production` (GitHub) để
  có nút duyệt nếu muốn. Sau deploy: smoke `curl https://cris.ahvlabs.com/api/health`.
- Tài liệu: `docs/deploy-prod.md` (kiến trúc máy chủ, secrets cần tạo, cách quay lui, cron,
  sao lưu, nâng cấp tay), `README.md` mục Triển khai, `BUILDING.md` link. Không đưa mật
  khẩu/khóa vào repo.

## I2 — Tệp minh chứng (upload)

- Lưu tệp ở `CRIS_DATA_DIR` (mặc định `/data`, volume `cris_data` trong compose), đường dẫn
  `evidence/<declaration_id>/<id>-<tên-an-toàn>`; giới hạn 10 MB; loại cho phép
  `pdf,png,jpg,jpeg,docx`; băm SHA-256 lưu vào `evidence.note`? — **không**: thêm migration
  `0011_evidence_file.sql`: cột `evidence.storage_path text`, `size bytes int`, `sha256 text`,
  `content_type text`.
- `cris/declare.py`: `add_evidence_file(conn, declaration_id, *, upload, actor_id, note)`
  (nhận bytes + tên + content-type; kiểm loại bằng chữ ký byte đầu, không tin phần mở
  rộng), `get_evidence(conn, evidence_id)` (kiểm quyền đơn vị như hồ sơ).
- API: `POST /api/declarations/{id}/evidence/file` (multipart, `python-multipart` — thêm
  dependency MIT vào `pyproject` + `DEPENDENCIES.md`), `GET /api/evidence/{id}/file`
  (`FileResponse`, `Content-Disposition` attachment, cần đăng nhập khi auth bật, cấp khoa
  chỉ đơn vị mình). Xoá không hỗ trợ (minh chứng là bằng chứng; ghi rõ).
- Test ≥ 8 (kích thước, loại, băm, quyền, tải về). Compose thêm volume `cris_data:/data`;
  `Dockerfile` `mkdir /data && chown cris`.

## I3 — AI gợi ý người hướng dẫn cho đồ án `ICTU_TEACHER`

- `cris/ai/mentor.py`: `suggest_mentors(conn, provider, *, k=5, min_votes=2)`: với mỗi đồ
  án sống có lượt tên vai `mentor` là `is_placeholder`, lấy top-k đồ án gần nhất (vector đã
  có) **có người hướng dẫn thật đã liên kết**; đếm phiếu theo `person_id` của người hướng dẫn
  láng giềng, trọng số = cosine; ghi `ai_suggestion(kind='mentor', target_id=work_id,
  payload={candidates:[{person_id, display_name, votes, score, evidence:[work_id...]}]})`
  (migration `0012` mở CHECK `kind` thêm `mentor`). Trả `{scanned, suggested}`.
- CLI `python -m cris ai mentors`. API `GET /api/ai/mentors?unit=&min_votes=&page=` (đồ án +
  ứng viên), `POST /api/ai/mentors/{work_id}/accept {person_id}` (rd_officer): tạo
  `author_link` `ChoXacNhan` cho lượt tên mentor giữ chỗ với `confidence='ai_mentor'`,
  `basis={ai:true, votes}` — **qua hàm có sẵn của `cris/link.py`** nếu có hàm chèn ứng viên;
  nếu không có, thêm hàm mới `link.add_candidate(conn, mention_id, person_id, confidence,
  basis)` (ngoại lệ được phép, tối thiểu) — quyết định cuối vẫn ở hàng đợi tác giả (BR-18).
- Test ≥ 8 với FakeProvider. Thử thật: số đồ án được gợi ý, 5 ví dụ.

## Codex I0 — Hướng dẫn sử dụng trong ứng dụng

- Trang `/huong-dan/`: mục lục theo vai trò (Phòng KH-CN / Khoa / Giảng viên / Lãnh đạo), mỗi
  mục 3–6 bước có ảnh (`public/huong-dan/*.png` chép từ `docs/images`), thuật ngữ (xuất xứ,
  hàng đợi, nghi trùng, khía cạnh, chế độ mở), "AI làm gì và không làm gì". Link ở sidebar
  (cuối) và ở trang đăng nhập. Nội dung tiếng Việt, không lorem, lấy số liệu thật từ README.

## Codex I — giao diện cho I2 + I3

- Minh chứng: kéo-thả tệp trong Dialog thêm minh chứng, hiển thị kích thước/loại/băm rút gọn,
  nút tải về; danh sách minh chứng ở `/ke-khai/`.
- `/doi-soat/huong-dan/` (menu "Hàng đợi tác giả" tab thứ hai "Gợi ý người hướng dẫn"):
  bảng đồ án ICTU_TEACHER → ứng viên (tên, học vị, phiếu, điểm, 2 đồ án dẫn chứng link),
  nút "Đưa vào hàng đợi xác nhận" → POST accept → toast; khi đã có link chờ thì hiển thị
  "Đang chờ xác nhận" link tới hàng đợi.

## Ngoài kế hoạch

SSO, xoá minh chứng, thông báo email, biểu mẫu Bộ.

## Kết quả (11/09/2026)

| Việc | Commit | Ghi chú |
|---|---|---|
| Chuẩn bị ảnh `-ai` cho GHCR (cần cho `wait-image` của I1 và cho `ai mentors` chạy trên máy chủ) | `6ff35e7` `ci(docker): publish an -ai image variant with the AI extra preinstalled` | Làm trước khi viết plan này — ảnh không-AI có sẵn nhưng thiếu extra `[ai]` |
| Sửa `ai download` để tôn trọng `CRIS_AI_MODEL_DIR` | `5893096` `fix(cli): ai download honours CRIS_AI_MODEL_DIR so the model lands in the mounted volume` | Phát hiện khi thử triển khai thật: mô hình tải vào cache người dùng thay vì volume mount, mất khi xoá container — xem "Điều lệch" |
| I1 — kế hoạch chi tiết lát cắt I | `fd6b547` `docs(plan): slice I — production CI/CD, evidence files, AI mentor suggestions, in-app user guide` | Tệp kế hoạch này |
| I1 — `deploy.yml` + `upgrade.sh` (sao lưu, migrate, health check, quay lui) | `12ba8d6` `ci(deploy): production deploy workflow and upgrade script with backup and rollback` | |
| I1 — ảnh `-ai` thiếu trên GHCR chỉ cảnh báo, không chặn deploy | `e725622` `ci(deploy): missing -ai image on GHCR is a warning, the server builds it` | Điều lệch so kế hoạch gốc (chặn 10 phút rồi lỗi) — xem "Điều lệch" |
| I2 — tải/tải về minh chứng dạng tệp, băm SHA-256, kiểm loại | `2386a88` `feat(declare): evidence file uploads with type sniffing, hashing and unit-scoped download` | |
| I2 — volume `cris_data` bền vững trong compose | `4ef0c5f` `deploy(compose): persistent volume for evidence files` | |
| Codex I0 — trang `/huong-dan/` | `4a92101` `feat(frontend): in-app user guide by role with screenshots, glossary and FAQ` | |
| I1 — rút thời gian chờ ảnh GHCR xuống 5 phút | `8dff91e` `ci(deploy): shorten the GHCR image wait to five minutes` | Kế hoạch ghi 10 phút, `docs/deploy-prod.md` ghi 15 phút lúc viết — giá trị chạy thật cuối cùng là 5 phút vì ảnh dựng xong sớm hơn ước tính |
| Sửa tên hiển thị giảng viên viết hoa chữ cái đầu | `365689d` `fix(people): title-case lecturer display names from the source display field` | Phát hiện phụ khi kiểm dữ liệu thật cho `ai mentors`/hướng dẫn sử dụng, không thuộc I1–I4 |
| I1 — bước preflight kiểm khoá SSH/known_hosts/kết nối | `09df4df` `ci(deploy): preflight step validates the SSH key, known_hosts and connectivity with clear errors` | Thêm sau khi lượt deploy đầu tiên thất bại vì secrets — xem "Điều lệch" |
| I1 — giữ bản sao lưu trước nâng cấp cạnh bản sao lưu đêm | `1229f3f` `deploy(upgrade): keep pre-upgrade dumps next to the nightly backups` | |
| I3 — gợi ý người hướng dẫn (`cris/ai/mentor.py`, API, CLI) | `f8cdf40` `feat(ai): mentor suggestions for placeholder-supervised theses, routed through the author queue` | Chạy thử đầu tiên trên DB thật: `scanned=0 suggested=0` — xem "Điều lệch" |
| I4 (mới, ngoài kế hoạch gốc) — lượt tên vai `mentor` giữ chỗ từ `meta.GVHD`, `normalize --redo` | `84b39cc` `feat(normalize): placeholder mentor mentions from the GVHD meta field; normalize --redo` | Sinh ra trực tiếp từ phát hiện ở I3 |
| Codex I — giao diện minh chứng tệp + tab gợi ý người hướng dẫn | `fe83fa9` `feat(frontend): evidence file upload with hashes and download, AI mentor suggestion tab feeding the author queue` | |

### Điều lệch so với kế hoạch

- **GHCR thiếu ảnh `-ai` lúc `wait-image` chạy**: kế hoạch giả định ảnh luôn có sẵn trong
  cửa sổ chờ; thực tế lần chạy đầu `docker manifest inspect` không thấy ảnh (dựng ảnh `-ai`
  song song với lúc tag được đẩy, có thể chưa xong). Xử lý bằng hai thay đổi cùng lúc:
  `docker.yml` publish thêm biến thể `-ai` (`6ff35e7`, làm trước khi có kế hoạch này) và
  `wait-image` đổi từ chặn cứng sang chỉ cảnh báo (`e725622`) — `deploy/upgrade.sh` trên máy
  chủ tự dựng ảnh tại chỗ khi GHCR chưa có, không chặn toàn bộ lượt triển khai.
- **I3 chạy thử đầu tiên trả về `scanned=0 suggested=0`**: thuật toán `suggest_mentors` đúng
  theo kế hoạch và đủ test với `FakeProvider`, nhưng trên DB thật không đồ án nào có lượt
  tên vai `mentor` giữ chỗ — nguồn `cris/source/repository.py` chỉ dựng `archive.mentors` từ
  thẻ `<a class="lv-mentor-link">`, trang giữ chỗ `ICTU_TEACHER` không có thẻ này nên
  `normalize.extract_fields` (bản trước I4) không tạo lượt tên nào cho 4.655/5.375 đồ án.
  Phát hiện này sinh ra lát cắt **I4** (không có trong kế hoạch gốc): đọc thêm
  `meta["GVHD"]` khi `archive.mentors` rỗng, cộng CLI `normalize --redo` để vá hồi tố các
  `work` đã chuẩn hoá từ trước khi có nhánh này (`normalize_pending` mặc định chỉ xử lý
  phần đang chờ). Sau I4: 4.621 lượt tên giữ chỗ mới, `ai mentors` cho `suggested=1381`.
- **`ai download` bug**: `python -m cris ai download` tải mô hình vào cache người dùng mặc
  định thay vì `CRIS_AI_MODEL_DIR` như provider `local` khi nạp — phát hiện lúc thử triển
  khai thật bằng `docker compose run --rm app ai download`: container bị xoá sau khi chạy
  xong nên mô hình vừa tải cũng mất theo, lần `serve` sau phải tải lại. Sửa ở `5893096` để
  cả hai đường dẫn dùng chung biến môi trường.
- **Deploy #1 thất bại do secrets**: lượt chạy `deploy.yml` đầu tiên trên môi trường thật
  thất bại ở bước SSH — thông báo lỗi mặc định (`ssh-action`/`ssh` thuần) không đủ rõ để
  xác định secret nào sai (khoá riêng dán nhầm thành khoá công khai, hay `PROD_KNOWN_HOSTS`
  thiếu dòng). Thêm bước "Kiểm tra secrets trước khi kết nối" (`09df4df`) chạy
  `ssh-keygen -y` để xác nhận `PROD_SSH_KEY` là khoá riêng hợp lệ, kiểm `known_hosts` có
  chứa `PROD_HOST` (tự `ssh-keyscan` bổ sung nếu thiếu, kèm cảnh báo kém an toàn hơn), rồi
  thử SSH thật trước khi chạy `upgrade.sh` — lỗi báo rõ thiếu secret nào thay vì một dòng
  lỗi SSH chung chung.
