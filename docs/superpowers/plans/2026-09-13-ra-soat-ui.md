# Rà soát UI/UX toàn hệ thống — 13/09/2026

Cách rà: (1) quét tự động 34 trang × 3 khổ (1280 / 820 / 390) × sáng-tối trên stack local với dữ
liệu thật (tràn ngang, chữ bị cắt, phần tử chồng nhau, nút < 32px trên điện thoại, input/nút thiếu
nhãn, lỗi console, request lỗi, axe-core WCAG 2.1 AA mức serious/critical); (2) xem ảnh chụp từng
trang. Báo cáo JSON và ảnh ở thư mục scratchpad `uiaudit/` (không commit). Ngưỡng theo AGENTS.md
Mandatory Workflow 8–9 và Design Rules.

## Phát hiện (ưu tiên giảm dần)

### A. Lỗi thật (bug)
1. **Nhãn nguồn thay chữ trong URL** — `lib/labels.ts` (`sourceLabel`, dòng ~113) thay `repository` →
   "kho dữ liệu ICTU" bằng regex trên cả chuỗi, nên URL khoá nguồn hiện thành
   `https://kho dữ liệu ICTU.ictu.edu.vn/luan-an/...` ở bảng "Xuất xứ dữ liệu" (chi tiết công trình),
   lịch sử đồng bộ, nhật ký. Chỉ dịch khi cả chuỗi là mã nguồn; không đụng URL.
2. **Bản đồ tri thức: "Tô màu theo" hiện giá trị thô `topic`** thay vì "Chủ đề" (`SelectValue` không có
   render nhãn) — `app/ban-do/page.tsx`.
3. **`/ke-khai-cua-toi/` ở chế độ mở hoặc tài khoản không phải giảng viên**: gọi `/api/me/works` bị 409,
   trang hiện lỗi đỏ + "Thử lại" và log console. Phải là trạng thái rỗng thân thiện: "Trang này dành cho
   giảng viên có tài khoản; đăng nhập bằng tài khoản giảng viên để xem công trình của mình" (không gọi
   API khi biết trước không phải lecturer).
4. **Tra cứu (1280px, có thanh bên)**: bảng kết quả rộng hơn khung → tiêu đề cột "Trích dẫn" bị cắt,
   phải cuộn ngang trong bảng. Cột tiêu đề phải co được (`min-w-0`, `w-full`, `break-words`), ẩn cột
   "Trạng thái" dưới `xl`, nút trích dẫn dạng icon; không cuộn ngang ở ≥1280.
5. **Tra cứu (390px)**: ô "Bộ lọc nâng cao" và các select chồng lên nhau/lên kết quả ("Khoá" × "Điểm",
   "Chủ đề" × tiêu đề kết quả) — kiểm tra grid `sm:grid-cols-2` của hàng lọc chính và grid trong
   `<details>` ở 390px.
6. **Hướng dẫn (390px)**: các thẻ số "7.618 công trình" × "410 giảng viên", "4.621/5.375 đồ án" ×
   "ICTU_TEACHER" chồng lên nhau — flex không wrap.
7. **axe serious**: `color-contrast` ở `/doi-chieu/ra-soat/` (528 nút — chắc chắn một token dùng lặp: chữ
   muted trên nền badge/hàng tô màu), `/doi-soat/tac-gia/` (4), `/doi-soat/trung-lap/` (3),
   `/ky-bao-cao/chi-tiet/` (3), `/ke-khai-cua-toi/` (2), `/ban-do/` (2 — chú giải màu),
   `/doi-soat/huong-dan/` (1); `definition-list`/`dlitem` ở `/huong-dan/` và `/ve/` (16 — `<dl>` có phần
   tử bọc không hợp lệ giữa `dt/dd`); `scrollable-region-focusable` ở `/ky-bao-cao/chi-tiet/` (vùng cuộn
   không focus được). Chạy `npx playwright test e2e/a11y*` hoặc script audit để xác nhận về 0.
8. **Checkbox hàng đợi tác giả không có nhãn** (51 input/trang) — thêm `aria-label="Chọn <tên thô> – <công
   trình>"`; checkbox chọn tất cả cũng vậy. Các `input 1×1` còn lại là input ẩn của Select (bỏ qua).

### B. Trải nghiệm (UX)
9. **Tổng quan**: khối "Mới cập nhật từ kho" liệt kê 20 mục "Đổi" đẩy biểu đồ xuống hơn hai màn hình,
   cột "Thêm (0)" là hộp trống lớn. Giới hạn mỗi cột 5 mục + "Xem tất cả (20)" mở lịch sử đồng bộ;
   cột trống thu gọn thành một dòng. Trên điện thoại, 4 thẻ số xếp dọc rất cao (biểu tượng chiếm một
   dòng riêng) → lưới 2 cột, biểu tượng nằm cạnh tiêu đề.
10. **Tra cứu trên điện thoại**: 7 select xếp dọc trước kết quả, phải cuộn qua ~1,5 màn hình mới thấy
    kết quả. Ở `< lg`: chỉ hiện ô từ khoá + cách tìm + nút; các bộ lọc nằm trong nút "Bộ lọc (n đang
    áp dụng)" mở/đóng (Collapsible), nhớ trạng thái mở; chip bộ lọc đang áp dụng hiện dưới ô tìm.
11. **Chi tiết công trình – bảng Xuất xứ**: (a) cột Nguồn lặp nguyên văn URL rất dài cho từng dòng →
    hiện "Kho ICTU · 10/09/2026 16:38" + biểu tượng liên kết ngoài (tooltip/URL đầy đủ); (b) các dòng
    cả hai giá trị đều "—" và "Chưa ghi nhận nguồn" (DOI, Tạp chí, Tập/số… với luận án) làm bảng dài
    vô ích → gom vào "Hiện n trường chưa có dữ liệu" (mặc định ẩn); (c) ở 390px, bảng 4 cột cuộn ngang
    với "Giá trị gốc" bị cắt → chuyển sang dạng thẻ xếp dọc (Trường / Đang dùng / Gốc / Nguồn).
12. **Hàng đợi tác giả**: một công trình có 3 ứng viên trùng tên hiện thành 3 dòng giống hệt nhau, người
    dùng khó hiểu phải chọn gì. Gộp các dòng cùng (tên thô, công trình) thành một khối: tiêu đề công trình
    một lần, các ứng viên là các lựa chọn radio kèm học vị/khoa/số công trình đã xác nhận, nút "Xác
    nhận" áp dụng cho ứng viên đang chọn. Cột "Gợi ý AI" chỉ hiện khi có gợi ý thật (bỏ dòng "chưa có
    công trình đã xác nhận" lặp lại). Trên điện thoại: dạng thẻ, không bảng cuộn ngang.
13. **Footer nguồn dữ liệu ở 1280px** bị cắt "… Apache-2.0 — ICT…". Cho phép 2 dòng khi < 1440 (tăng
    `--data-footer-h` tương ứng) hoặc rút ngắn: "Dữ liệu từ DSpace ICTU (repository.ictu.edu.vn), chỉ
    dùng cho học tập · Apache-2.0".
14. **Nút/tab quá nhỏ trên điện thoại** (< 32px): tab segmented ở Bản đồ (25px), Đối soát (25px), Kỳ báo
    cáo (25px), Cảnh báo (28px), nút sắp xếp tiêu đề cột (20px), "Xem lịch sử đồng bộ"/"RSS" (28px).
    Trên `< sm` dùng `h-9` (36px) cho tab và nút phụ; nút sắp xếp cột tối thiểu 32px vùng bấm.
15. **Đăng nhập**: trang hiện cùng thanh bên đầy đủ menu (ở chế độ mở); khi `auth_required` và chưa đăng
    nhập, thanh bên chỉ nên có mục công khai (Tra cứu, Chủ đề, Bản đồ, Kiểm tra đề tài, Hướng dẫn, Về).
    Kiểm bằng fixture `auth_required: true` không user.

### C. Ghi nhận, không sửa đợt này
- Các cảnh báo "chồng" giữa nội dung và footer là do footer cố định che đáy khung nhìn trong ảnh
  fullPage — người dùng cuộn thì không sao; đã kiểm padding-bottom của nội dung bằng `--data-footer-h`.
- Ảnh đại diện chỉ 6/400 có ở kho; còn lại chữ cái đầu (giống kho).

## Codex UI-1 — sửa
Toàn bộ mục A và B (1–15). Không đổi API, không thêm dependency. Mỗi mục sửa xong tự kiểm ở 390/820/1280,
sáng/tối; chạy `npm run lint && npx tsc --noEmit && NEXT_PUBLIC_API_BASE= npm run build`, Playwright mock
và thật đều xanh; thêm/điều chỉnh e2e cho mục 3, 9, 10, 12, 15. Thêm `scripts/ui-audit.mjs` (SPDX, chạy
`node scripts/ui-audit.mjs http://localhost:8000 <thư-mục-ra>`) dựa trên script quét ở trên để lần sau
rà lại được; ghi cách chạy vào `frontend/AGENTS.md`.

## Kết quả
(điền sau)
