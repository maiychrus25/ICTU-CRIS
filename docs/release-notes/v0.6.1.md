# ICTU-CRIS v0.6.1

> Phát hành 13/09/2026. Tag `v0.6.1`; ảnh `ghcr.io/maiychrus25/ictu-cris:0.6.1` và `:0.6.1-ai` (kèm thư viện AI).

Bản vá nhỏ sau 0.6.0: nhận diện sản phẩm và SEO cho phần công khai, tài liệu hướng dẫn sử dụng
đầy đủ, và một sửa lỗi ở quy trình triển khai tự động.

## Điểm nhấn

- **Logo sản phẩm** ở mọi nơi: thanh bên, đăng nhập, cổng kiểm tra đề tài, hướng dẫn, lý lịch
  khoa học (bản in), Về hệ thống; bộ favicon, ảnh Open Graph 1200×630 khi chia sẻ liên kết,
  `manifest.webmanifest`.
- **SEO** cho trang công khai: metadata từng trang (title "· ICTU-CRIS", description có từ khoá,
  Open Graph/Twitter, canonical, theme-color), JSON-LD `WebSite` (tìm kiếm → `/tra-cuu/`),
  `Organization`, `SoftwareApplication`; `robots.txt` (9 trang công khai được index, trang nội bộ
  và `/api/` không) và `sitemap.xml` sinh tĩnh cùng `next build`.
- **Tài liệu hướng dẫn sử dụng** `docs/huong-dan-su-dung.md`: 19 tính năng theo menu (là gì ·
  cung cấp gì · cách dùng · ai được dùng · lưu ý), 3 quy trình xuyên suốt, bảng AI, FAQ, thuật ngữ.

## Đã sửa (Fixed)

- `deploy/upgrade.sh`: tệp chưa theo dõi trên máy chủ mà tag mới bắt đầu quản lý được cất sang
  `.local.bak` trước `git checkout` — Deploy tự động v0.6.0 từng thất bại vì `deploy/pipeline.sh`
  chép tay.

## Đã kiểm chứng

| Hạng mục | Kết quả |
|---|---|
| Test Python | 433 passed, 3 skipped (không đổi) |
| Playwright dữ liệu mẫu / dữ liệu thật | xanh (thêm kịch bản nhận diện & SEO) / 18/18 |
| `out/robots.txt`, `out/sitemap.xml`, `icon.svg`, `og-image.png`, `manifest.webmanifest` | phục vụ 200 trên ảnh đa tầng |

## Nâng cấp từ 0.6.0

Không có migration mới; chỉ đổi ảnh. Sau khi lên, gửi `https://cris.ahvlabs.com/sitemap.xml`
cho Google Search Console nếu muốn được index.
